// constants.dart
import 'package:flutter/material.dart';

const double cardRadius = 16;
const double imageHeight = 120;
const double paddingSize = 12;
const Color priceColor = Colors.green;

const Color categoryTagColor = Colors.blue;

const List<BoxShadow> cardShadow = [
  BoxShadow(
    color: Color(0x14000000),
    blurRadius: 12,
    offset: Offset(0, 4),
    spreadRadius: 1,
  ),
];

const Color cardBackgroundColor = Color(0xFFFFFDF5);
